{% extends 'AppBundle:EmailTemplate:skeleton.html.twig' %}
{% block title %}{{ 'admin.emailTemplate.new.title'|trans }} | {{ parent() }}{% endblock %}
{% block content %}
    <div class="panel-heading">
        <h3 class="panel-title">{{ icon('files-o') }} {{ 'admin.emailTemplate.new.title'|trans }}</h3>
    </div>
    <div class="panel-body">
        {{ form(form) }}
    </div>
{% endblock %}
