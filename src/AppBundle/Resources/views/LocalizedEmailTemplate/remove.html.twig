{% extends 'AppBundle:LocalizedEmailTemplate:skeleton.html.twig' %}
{% block title %}{{ 'admin.localizedEmailTemplate.delete.title'|trans({'%name%': emailTemplate.name, '%locale%': localizedEmailTemplate.locale}) }} | {{ parent() }}{% endblock %}
{% block content %}
    <div class="panel-heading">
        <h3 class="panel-title">{{ icon('files-o') }} {{ 'admin.localizedEmailTemplate.delete.title'|trans({'%name%': emailTemplate.name, '%locale%': localizedEmailTemplate.locale}) }}</h3>
    </div>
    <div class="panel-body">
        {{ form(form) }}
    </div>
{% endblock %}
