{% extends 'AppBundle:LocalizedEmailTemplate:skeleton.html.twig' %}
{% block title %}{{ 'admin.localizedEmailTemplate.new.title'|trans({'%name%': emailTemplate.name}) }} | {{ parent() }}{% endblock %}
{% block content %}
    <div class="panel-heading">
        <h3 class="panel-title">{{ icon('files-o') }} {{ 'admin.localizedEmailTemplate.new.title'|trans({'%name%': emailTemplate.name}) }}</h3>
    </div>
    <div class="panel-body">
        {{ form(form) }}
    </div>
{% endblock %}
