{% extends 'AppBundle:Admin/ApiUser:skeleton.html.twig' %}
{% block title %}{{ 'admin.user.edit.title'|trans({'%name%': user.username}) }} | {{ parent() }}{% endblock %}
{% block content %}
    <div class="panel-heading">
        <h3 class="panel-title">{{ icon('list-alt') }} {{ 'admin.user.edit.title'|trans({'%name%': user.username}) }}</h3>
    </div>
    <div class="panel-body">
        {{ form(form) }}
    </div>
{% endblock %}
