{% extends 'AppBundle:Admin/EmailAddress:skeleton.html.twig' %}
{% block title %}{{ 'admin.emailAddress.new.title'|trans }} | {{ parent() }}{% endblock %}
{% block content %}
    <div class="panel-heading">
        <h3 class="panel-title">{{ icon('list-alt') }} {{ 'admin.emailAddress.new.title'|trans }}</h3>
    </div>
    <div class="panel-body">
        {{ form(form) }}
    </div>
{% endblock %}
