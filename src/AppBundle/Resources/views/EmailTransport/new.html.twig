{% extends 'AppBundle:EmailTransport:skeleton.html.twig' %}
{% block title %}{{ 'admin.emailTransport.new.title'|trans }} | {{ parent() }}{% endblock %}
{% block content %}
    <div class="panel-heading">
        <h3 class="panel-title">{{ icon('exchange') }} {{ 'admin.emailTransport.new.title'|trans }}</h3>
    </div>
    <div class="panel-body">
        {{ form(form) }}
    </div>
{% endblock %}
